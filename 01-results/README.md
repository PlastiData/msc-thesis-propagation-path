# Results (frozen)

Open **`propagation-paths/strict/index.html`** (relaxed twin under `relaxed/`).
Case folders are per-failure detail.

LLM second opinion: `llm-second-opinion/relaxed/index.html` and `pilot_results.json`.
